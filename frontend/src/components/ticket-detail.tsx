'use client';

import Link from 'next/link';
import { useEffect, useState } from 'react';
import { getCurrentUser } from '@/lib/auth';
import { apiMessage, getTicket, performTicketAction } from '@/lib/ticket-api';
import { supervisorRoles, ticketPriorities, ticketStatuses, type Ticket, type TicketAction, type TicketActionInput, type TicketPriority } from '@/lib/ticket-types';
import type { Role } from '@/lib/types';
import { EmptyState, ErrorState, LoadingState } from './ui';

function label(value: string) { return value.replaceAll('_', ' ').toLowerCase().replace(/\b\w/g, (letter) => letter.toUpperCase()); }
const actionForStatus: Partial<Record<string, { action: TicketAction; label: string }>> = { OPEN: { action: 'accept', label: 'Accept Ticket' }, ASSIGNED: { action: 'start', label: 'Start Work' } };

export function TicketDetailView({ ticketId }: { ticketId: string }) {
  const [ticket, setTicket] = useState<Ticket | null>(null); const [role, setRole] = useState<Role>('operator'); const [error, setError] = useState(''); const [actionError, setActionError] = useState(''); const [busy, setBusy] = useState(false); const [notes, setNotes] = useState<TicketActionInput>({}); const [showResolve, setShowResolve] = useState(false); const [technicianId, setTechnicianId] = useState(''); const [priority, setPriority] = useState<TicketPriority>('MEDIUM');
  useEffect(() => { getTicket(ticketId).then(setTicket).catch((reason) => setError(apiMessage(reason))); getCurrentUser().then((result) => { if (result.status === 'authenticated' && result.user.role) setRole(result.user.role); }); }, [ticketId]);
  if (error) return <ErrorState title="Ticket unavailable" description={error} />;
  if (!ticket) return <LoadingState label="Loading ticket" />;
  const isTechnician = role === 'technician'; const isSupervisor = supervisorRoles.includes(role); const statusAction = actionForStatus[ticket.status];
  async function action(actionType: TicketAction, input: TicketActionInput = {}) { setActionError(''); setBusy(true); try { setTicket(await performTicketAction(ticket!.id, actionType, input)); setShowResolve(false); } catch (reason) { setActionError(apiMessage(reason)); } finally { setBusy(false); } }
  function resolve() { if (!notes.diagnosis?.trim() || !notes.actionTaken?.trim() || !notes.repairNotes?.trim() || !notes.completionTime) { setActionError('Diagnosis, action taken, repair notes, and completion time are required before resolving.'); return; } action('resolve', notes); }
  return <div className="ticket-detail-layout"><main><div className="ticket-detail-hero"><div><p className="eyebrow">{ticket.number}</p><h1>{ticket.problemType}</h1><p>{ticket.machine.code} / {ticket.machine.name} / {ticket.plant}</p></div><b className={`ticket-status ticket-status--${ticket.status.toLowerCase()}`}>{label(ticket.status)}</b></div>{ticket.sourceType && <section className="source-card"><div><span className="source-label">Source</span><strong className={`source-badge source-badge--${ticket.sourceType.toLowerCase()}`}>{ticket.sourceType}</strong></div>{ticket.sourceType === 'QA' && ticket.sourceId && <Link href={`/qa/defects/${ticket.sourceId}`}>View QA Defect {ticket.sourceId}</Link>}</section>}<section className="ticket-info-card"><h2>Ticket information</h2><dl className="ticket-facts"><div><dt>Machine</dt><dd>{ticket.machine.code} / {ticket.machine.name}</dd></div><div><dt>Location</dt><dd>{ticket.location}</dd></div><div><dt>Reporter</dt><dd>{ticket.reporter?.name ?? 'Provided by Laravel'}</dd></div><div><dt>Priority</dt><dd><b className={`priority-dot priority-dot--${ticket.priority.toLowerCase()}`} />{label(ticket.priority)}</dd></div><div><dt>Created</dt><dd>{ticket.createdAt}</dd></div><div><dt>Assigned technician</dt><dd>{ticket.technician?.name ?? 'Unassigned'}</dd></div></dl><div className="ticket-description"><dt>Description</dt><p>{ticket.description}</p></div></section>{ticket.sla && <section className={`sla-card sla-card--${ticket.sla.status.toLowerCase()}`}><div><span>SLA status</span><strong>{label(ticket.sla.status)}</strong></div><div><span>Target</span><strong>{ticket.sla.targetAt ?? ticket.sla.dueAt ?? '--'}</strong></div><div><span>Remaining</span><strong>{ticket.sla.remaining ?? '--'}</strong></div><div><span>Overdue duration</span><strong>{ticket.sla.overdueDuration ?? '--'}</strong></div></section>}<section className="ticket-info-card"><div className="detail-card__heading"><h2>Audit trail</h2><span>SOURCE: LARAVEL</span></div><div className="ticket-timeline">{ticketStatuses.map((status) => { const event = ticket.timeline?.find((item) => item.status === status); const complete = ticket.timeline?.some((item) => item.status === status); return <div className={`timeline-step ${complete ? 'timeline-step--complete' : ''} ${status === ticket.status ? 'timeline-step--current' : ''}`} key={status}><span className="timeline-marker" /><div><strong>{label(status)}</strong>{event && <><small>{event.actor ?? 'Maintenance system'} / {event.createdAt}</small>{event.action && <small>Action: {event.action}</small>}{event.change && <p>{event.change}</p>}{event.note && <p>{event.note}</p>}</>}</div></div>; })}</div>{!ticket.timeline?.length && <EmptyState title="No audit events" description="Ticket history will appear when Laravel returns its audit timeline." />}</section><section className="ticket-info-card"><div className="detail-card__heading"><h2>Attachments</h2><span>{ticket.attachments?.length ?? 0}</span></div>{ticket.attachments?.length ? <div className="attachment-list">{ticket.attachments.map((attachment) => <a href={attachment.url} key={attachment.id}>{attachment.name}</a>)}</div> : <EmptyState title="No attachments" description="Photos and documents will appear here when provided." />}</section></main><aside className="ticket-actions"><Link className="back-link" href="/tickets">Back to tickets</Link><section className="action-card"><p className="eyebrow">Technician actions</p>{isTechnician && statusAction && <button className="primary-button" disabled={busy} onClick={() => action(statusAction.action)}>{busy ? 'Updating...' : statusAction.label}</button>}{isTechnician && ticket.status === 'IN_PROGRESS' && <><button className="secondary-action" onClick={() => setShowResolve(true)}>Resolve Ticket</button><button className="secondary-action" onClick={() => action('diagnosis', notes)}>Add Diagnosis</button><button className="secondary-action" onClick={() => action('action', notes)}>Add Action</button><button className="secondary-action" onClick={() => action('spare-part', notes)}>Add Spare Part</button><label className="upload-action">Upload Photo<input type="file" accept="image/*" onChange={(event) => event.target.files?.[0] && action('photo', { photo: event.target.files[0] })} /></label></>}{isSupervisor && <><label className="form-field">Technician ID<input value={technicianId} onChange={(event) => setTechnicianId(event.target.value)} placeholder="Laravel technician ID" /></label><button className="secondary-action" disabled={!technicianId || busy} onClick={() => action('assign', { technicianId })}>Assign / Reassign Technician</button><label className="form-field">Priority<select value={priority} onChange={(event) => setPriority(event.target.value as TicketPriority)}>{ticketPriorities.map((item) => <option key={item} value={item}>{label(item)}</option>)}</select></label><button className="secondary-action" disabled={busy} onClick={() => action('priority', { priority })}>Change Priority</button>{ticket.status === 'RESOLVED' && <><button className="secondary-action" onClick={() => action('review')}>Review Resolution</button><button className="primary-button" onClick={() => action('verify')}>Verify Resolution</button></>}{ticket.status === 'VERIFIED' && <button className="secondary-action" onClick={() => action('reopen')}>Reopen Ticket</button>}</>}{!isTechnician && !isSupervisor && <p className="action-note">Actions are available to technicians and supervisors according to Laravel authorization.</p>}{actionError && <ErrorState title="Action failed" description={actionError} />}</section>{showResolve && <section className="resolve-card"><h2>Resolve ticket</h2><p>Required repair information</p><label className="form-field">Diagnosis<textarea rows={3} value={notes.diagnosis ?? ''} onChange={(event) => setNotes({ ...notes, diagnosis: event.target.value })} /></label><label className="form-field">Action taken<textarea rows={3} value={notes.actionTaken ?? ''} onChange={(event) => setNotes({ ...notes, actionTaken: event.target.value })} /></label><label className="form-field">Repair notes<textarea rows={3} value={notes.repairNotes ?? ''} onChange={(event) => setNotes({ ...notes, repairNotes: event.target.value })} /></label><label className="form-field">Completion time<input type="datetime-local" value={notes.completionTime ?? ''} onChange={(event) => setNotes({ ...notes, completionTime: event.target.value })} /></label><button className="primary-button" onClick={resolve} disabled={busy}>Submit resolution</button></section>}</aside></div>;
}
